<?xml version='1.0' encoding='UTF-8'?>
<manifest>
<!--
SPDX-License-Identifier: LGPL-2.1-or-later
Copyright © 2017-2018 ANSSI. All rights reserved.
-->

  <!-- The Git remotes available for the manifest file. -->

  <!-- The following remote ("origin") sets "." (single dot) as a git fetch
       prefix. This implies that all the git repositories for this remote must
       sit at the same path level as the git repository containing this
       manifest file (i.e. the one you used with the command "repo init -u
       <URL_TO_MANIFEST_GIT_REPO>"). This trick seems to work only with HTTP(S)
       remote URIs. -->
  <remote fetch="." name="origin" review="" />

<!-- vim: set ts=2 sts=2 sw=2 et: -->
</manifest>
